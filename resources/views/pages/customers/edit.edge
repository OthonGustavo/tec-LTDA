@component('components/layout', { title: 'Editar cliente', active: 'customers' })
  <div class="mx-auto max-w-2xl">
    <div class="flex items-center gap-2 text-sm text-slate-500">
      <a href="{{ route('customers.index') }}" class="hover:text-slate-700">Clientes</a>
      <span>/</span>
      <a href="{{ route('customers.show', { id: customer.id }) }}" class="hover:text-slate-700">
        {{ customer.name }}
      </a>
      <span>/</span>
      <span class="text-slate-700">Editar</span>
    </div>

    <h1 class="mt-2 text-2xl font-bold tracking-tight text-slate-900">Editar cliente</h1>
    <p class="mt-1 text-sm text-slate-600">
      Atualize os dados cadastrais e o estágio do relacionamento.
    </p>

    <div class="mt-6 rounded-lg border border-slate-200 bg-white p-6">
      <form
        method="POST"
        action="{{ route('customers.update', { id: customer.id }) }}"
        class="space-y-5"
      >
        {{ csrfField() }}

        <div>
          <label for="name" class="block text-sm font-medium text-slate-700">Nome</label>
          <input
            id="name"
            type="text"
            name="name"
            value="{{ old('name', customer.name) }}"
            required
            class="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm text-slate-900 placeholder:text-slate-400 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
          />
          @inputError('name')
            <p class="mt-1 text-sm text-rose-600">{{ $messages[0] }}</p>
          @end
        </div>

        <div>
          <label for="email" class="block text-sm font-medium text-slate-700">E-mail</label>
          <input
            id="email"
            type="email"
            name="email"
            value="{{ old('email', customer.email) }}"
            placeholder="contato@empresa.com.br"
            class="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm text-slate-900 placeholder:text-slate-400 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
          />
          @inputError('email')
            <p class="mt-1 text-sm text-rose-600">{{ $messages[0] }}</p>
          @end
        </div>

        <div>
          <label for="phone" class="block text-sm font-medium text-slate-700">Telefone</label>
          <input
            id="phone"
            type="text"
            name="phone"
            value="{{ old('phone', customer.phone) }}"
            placeholder="(11) 90000-0000"
            class="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm text-slate-900 placeholder:text-slate-400 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
          />
          @inputError('phone')
            <p class="mt-1 text-sm text-rose-600">{{ $messages[0] }}</p>
          @end
        </div>

        <div>
          <label for="company" class="block text-sm font-medium text-slate-700">Empresa</label>
          <input
            id="company"
            type="text"
            name="company"
            value="{{ old('company', customer.company) }}"
            placeholder="Razão social ou nome fantasia"
            class="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm text-slate-900 placeholder:text-slate-400 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
          />
          @inputError('company')
            <p class="mt-1 text-sm text-rose-600">{{ $messages[0] }}</p>
          @end
        </div>

        <div>
          <label for="status" class="block text-sm font-medium text-slate-700">Status</label>
          <select
            id="status"
            name="status"
            class="mt-1 w-full rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm text-slate-900 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
          >
            @each(option in statusOptions)
              <option
                value="{{ option.value }}"
                {{ old('status', customer.status) === option.value ? 'selected' : '' }}
              >{{ option.label }}</option>
            @end
          </select>
          @inputError('status')
            <p class="mt-1 text-sm text-rose-600">{{ $messages[0] }}</p>
          @end
        </div>

        <div class="flex items-center gap-3 border-t border-slate-200 pt-5">
          <button
            type="submit"
            class="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-blue-700"
          >Salvar</button>

          <a
            href="{{ route('customers.show', { id: customer.id }) }}"
            class="text-sm font-medium text-slate-500 hover:text-slate-700"
          >Cancelar</a>
        </div>
      </form>
    </div>
  </div>
@end
