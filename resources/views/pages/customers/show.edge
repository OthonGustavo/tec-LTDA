@component('components/layout', { title: customer.name, active: 'customers' })
  <div class="mb-4">
    <a
      href="{{ route('customers.index') }}"
      class="text-sm font-medium text-slate-500 hover:text-slate-700"
    >← Clientes</a>
  </div>

  <div class="mb-6 flex flex-wrap items-start justify-between gap-4">
    <div>
      <div class="flex flex-wrap items-center gap-3">
        <h1 class="text-2xl font-bold tracking-tight text-slate-900">{{ customer.name }}</h1>
        <span
          class="inline-flex items-center rounded-full px-2.5 py-1 text-xs font-semibold ring-1 ring-inset {{ statusClasses[customer.status] }}"
        >{{ statusLabels[customer.status] }}</span>
      </div>
      @if(customer.company)
        <p class="mt-1 text-sm text-slate-500">{{ customer.company }}</p>
      @end
    </div>

    <div class="flex items-center gap-2">
      <a
        href="{{ route('customers.edit', { id: customer.id }) }}"
        class="rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50"
      >Editar</a>
      <form
        method="POST"
        action="{{ route('customers.destroy', { id: customer.id }) }}"
        onsubmit="return confirm('Excluir este cliente? As oportunidades e anotações dele também serão removidas.')"
      >
        {{ csrfField() }}
        <button
          type="submit"
          class="rounded-lg border border-rose-200 bg-white px-3 py-2 text-sm font-medium text-rose-700 hover:bg-rose-50"
        >Excluir</button>
      </form>
    </div>
  </div>

  <div class="mb-6 grid gap-4 sm:grid-cols-3">
    <div class="rounded-lg border border-slate-200 bg-white p-4">
      <p class="text-xs font-medium uppercase tracking-wide text-slate-500">Em negociação</p>
      <p class="mt-1 text-2xl font-bold text-slate-900">
        {{ new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(Number(openValue)) }}
      </p>
    </div>
    <div class="rounded-lg border border-slate-200 bg-white p-4">
      <p class="text-xs font-medium uppercase tracking-wide text-slate-500">Oportunidades</p>
      <p class="mt-1 text-2xl font-bold text-slate-900">{{ customer.opportunities.length }}</p>
    </div>
    <div class="rounded-lg border border-slate-200 bg-white p-4">
      <p class="text-xs font-medium uppercase tracking-wide text-slate-500">Anotações</p>
      <p class="mt-1 text-2xl font-bold text-slate-900">{{ customer.interactions.length }}</p>
    </div>
  </div>

  <div class="grid gap-6 lg:grid-cols-3">
    <aside class="lg:col-span-1">
      <div class="rounded-lg border border-slate-200 bg-white p-5">
        <h2 class="text-sm font-semibold uppercase tracking-wide text-slate-500">Dados de contato</h2>
        <dl class="mt-4 space-y-4 text-sm">
          <div>
            <dt class="text-slate-500">E-mail</dt>
            <dd class="mt-0.5 font-medium text-slate-900">
              @if(customer.email)
                <a href="mailto:{{ customer.email }}" class="text-blue-700 hover:underline">{{ customer.email }}</a>
              @else
                <span class="text-slate-400">—</span>
              @end
            </dd>
          </div>
          <div>
            <dt class="text-slate-500">Telefone</dt>
            <dd class="mt-0.5 font-medium text-slate-900">
              @if(customer.phone)
                {{ customer.phone }}
              @else
                <span class="text-slate-400">—</span>
              @end
            </dd>
          </div>
          <div>
            <dt class="text-slate-500">Empresa</dt>
            <dd class="mt-0.5 font-medium text-slate-900">
              @if(customer.company)
                {{ customer.company }}
              @else
                <span class="text-slate-400">—</span>
              @end
            </dd>
          </div>
          <div>
            <dt class="text-slate-500">Status</dt>
            <dd class="mt-1">
              <span
                class="inline-flex items-center rounded-full px-2.5 py-1 text-xs font-semibold ring-1 ring-inset {{ statusClasses[customer.status] }}"
              >{{ statusLabels[customer.status] }}</span>
            </dd>
          </div>
          <div>
            <dt class="text-slate-500">Cliente desde</dt>
            <dd class="mt-0.5 font-medium text-slate-900">
              {{ customer.createdAt.setLocale('pt-BR').toFormat('dd/MM/yyyy') }}
            </dd>
          </div>
        </dl>
      </div>
    </aside>

    <div class="space-y-6 lg:col-span-2">
      <section class="rounded-lg border border-slate-200 bg-white p-5">
        <h2 class="text-base font-semibold text-slate-900">Oportunidades</h2>
        <p class="mt-1 text-sm text-slate-500">Negócios em andamento com este cliente.</p>

        <form
          method="POST"
          action="{{ route('opportunities.store', { id: customer.id }) }}"
          class="mt-4 grid gap-4 rounded-lg bg-slate-50 p-4 sm:grid-cols-6"
        >
          {{ csrfField() }}

          <div class="sm:col-span-3">
            <label for="title" class="block text-sm font-medium text-slate-700">Título</label>
            <input
              type="text"
              id="title"
              name="title"
              value="{{ old('title', '') }}"
              placeholder="Ex.: Implantação do sistema"
              class="mt-1 block w-full rounded-lg border border-slate-300 px-3 py-2 text-sm shadow-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
            />
            @inputError('title')
              <p class="mt-1 text-sm text-rose-600">{{ $messages[0] }}</p>
            @end
          </div>

          <div class="sm:col-span-1">
            <label for="estimatedValue" class="block text-sm font-medium text-slate-700">Valor estimado</label>
            <input
              type="number"
              step="0.01"
              min="0"
              id="estimatedValue"
              name="estimatedValue"
              value="{{ old('estimatedValue', '') }}"
              placeholder="0.00"
              class="mt-1 block w-full rounded-lg border border-slate-300 px-3 py-2 text-sm shadow-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
            />
            @inputError('estimatedValue')
              <p class="mt-1 text-sm text-rose-600">{{ $messages[0] }}</p>
            @end
          </div>

          <div class="sm:col-span-1">
            <label for="stage" class="block text-sm font-medium text-slate-700">Etapa</label>
            <select
              id="stage"
              name="stage"
              class="mt-1 block w-full rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm shadow-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
            >
              @each(option in stageOptions)
                <option value="{{ option.value }}" {{ old('stage', '') === option.value ? 'selected' : '' }}>{{ option.label }}</option>
              @end
            </select>
            @inputError('stage')
              <p class="mt-1 text-sm text-rose-600">{{ $messages[0] }}</p>
            @end
          </div>

          <div class="flex items-end sm:col-span-1">
            <button
              type="submit"
              class="w-full rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-700"
            >Adicionar</button>
          </div>
        </form>

        @if(customer.opportunities.length)
          <ul class="mt-5 divide-y divide-slate-200 border-t border-slate-200">
            @each(o in customer.opportunities)
              <li class="flex flex-wrap items-center justify-between gap-3 py-3">
                <div class="min-w-0">
                  <div class="flex flex-wrap items-center gap-2">
                    <p class="font-medium text-slate-900">{{ o.title }}</p>
                    <span
                      class="inline-flex items-center rounded-full px-2 py-0.5 text-xs font-semibold ring-1 ring-inset {{ stageClasses[o.stage] }}"
                    >{{ stageLabels[o.stage] }}</span>
                  </div>
                  <p class="mt-1 text-sm text-slate-500">
                    Criada em {{ o.createdAt.setLocale('pt-BR').toFormat('dd/MM/yyyy') }}
                  </p>
                </div>

                <div class="flex items-center gap-3">
                  <span class="text-sm font-semibold text-slate-900">
                    {{ new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(Number(o.estimatedValue)) }}
                  </span>
                  <a
                    href="{{ route('opportunities.edit', { id: o.id }) }}"
                    class="text-sm font-medium text-blue-700 hover:underline"
                  >Editar</a>
                  <form
                    method="POST"
                    action="{{ route('opportunities.destroy', { id: o.id }) }}"
                    onsubmit="return confirm('Excluir esta oportunidade?')"
                  >
                    {{ csrfField() }}
                    <button type="submit" class="text-sm font-medium text-rose-600 hover:underline">Excluir</button>
                  </form>
                </div>
              </li>
            @end
          </ul>
        @else
          <p class="mt-5 rounded-lg border border-dashed border-slate-300 px-4 py-6 text-center text-sm text-slate-500">
            Nenhuma oportunidade registrada para este cliente.
          </p>
        @end
      </section>

      <section class="rounded-lg border border-slate-200 bg-white p-5">
        <h2 class="text-base font-semibold text-slate-900">Histórico de interações</h2>
        <p class="mt-1 text-sm text-slate-500">Registre ligações, reuniões e combinados.</p>

        <form
          method="POST"
          action="{{ route('interactions.store', { id: customer.id }) }}"
          class="mt-4 rounded-lg bg-slate-50 p-4"
        >
          {{ csrfField() }}

          <label for="note" class="block text-sm font-medium text-slate-700">Nova anotação</label>
          <textarea
            id="note"
            name="note"
            rows="3"
            placeholder="Ex.: Ligação realizada, agendou reunião para terça."
            class="mt-1 block w-full rounded-lg border border-slate-300 px-3 py-2 text-sm shadow-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
          >{{ old('note', '') }}</textarea>
          @inputError('note')
            <p class="mt-1 text-sm text-rose-600">{{ $messages[0] }}</p>
          @end

          <div class="mt-3 flex justify-end">
            <button
              type="submit"
              class="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-700"
            >Registrar anotação</button>
          </div>
        </form>

        @if(customer.interactions.length)
          <ol class="mt-5 space-y-4 border-t border-slate-200 pt-5">
            @each(i in customer.interactions)
              <li class="flex gap-3 border-l-2 border-slate-200 pl-4">
                <div class="min-w-0 flex-1">
                  <p class="whitespace-pre-line text-sm text-slate-800">{{ i.note }}</p>
                  <p class="mt-1 text-xs text-slate-500">
                    {{ i.user ? (i.user.fullName || i.user.email) : 'Usuário removido' }}
                    ·
                    {{ i.createdAt.setLocale('pt-BR').toFormat('dd/MM/yyyy HH:mm') }}
                  </p>
                </div>
                <form
                  method="POST"
                  action="{{ route('interactions.destroy', { id: i.id }) }}"
                  onsubmit="return confirm('Remover esta anotação?')"
                >
                  {{ csrfField() }}
                  <button type="submit" class="text-xs font-medium text-rose-600 hover:underline">Remover</button>
                </form>
              </li>
            @end
          </ol>
        @else
          <p class="mt-5 rounded-lg border border-dashed border-slate-300 px-4 py-6 text-center text-sm text-slate-500">
            Nenhuma anotação registrada ainda.
          </p>
        @end
      </section>
    </div>
  </div>
@end
